# Minimal Requirements for PMC-VQA SFT Pipeline
# Use this if you already have PyTorch installed with CUDA support
# or if you want to install PyTorch separately

transformers>=4.36.0
datasets>=2.16.0
accelerate>=0.25.0
peft>=0.7.0
bitsandbytes>=0.41.0
pandas>=2.0.0
tqdm>=4.65.0
sentencepiece>=0.1.99
safetensors>=0.4.0

